# Быстрый старт

## 🚀 Запуск в IntelliJ IDEA (самый простой способ)

### Шаг 1: Откройте Main.java
Файл: `src/main/java/org/example/Main.java`

### Шаг 2: Нажмите Run
- Найдите зелёный треугольник слева от `public static void main`
- Правый клик → **"Run 'Main.main()'"** (или **Ctrl+Shift+F10**)

### Шаг 3: Настройте аргументы
- Вверху экрана: выпадающий список конфигураций → **"Edit Configurations..."**
- В поле **"Program arguments"** введите: `-i test.conf`
- Нажмите **OK**

### Шаг 4: Запустите снова
- Нажмите **Run** (зелёный треугольник) или **Shift+F10**
- Результат появится в консоли внизу

## 📝 Примеры файлов для тестирования

1. **test.conf** (простой тест в корне проекта)
2. **src/main/resources/examples/network.conf** (сетевая конфигурация)
3. **src/main/resources/examples/game.conf** (игровая конфигурация)

## 🧪 Запуск тестов

1. Откройте `src/test/java/org/example/ConfigLangTest.java`
2. Правый клик на классе → **"Run 'ConfigLangTest'"**
3. Или через терминал: `mvn test`

## 💻 Запуск через командную строку

```bash
# Компиляция
mvn compile

# Запуск
java -cp "target/classes;target/dependency/*" org.example.Main -i test.conf
```

Или используйте скрипт:
```bash
run.bat test.conf
```

## ✅ Что должно получиться

После запуска в консоли появится XML, например:
```xml
<config>
  <const name="x" value="10"/>
  <constExpr result="11">
    <add baseConst="x" baseValue="10" addend="1"/>
  </constExpr>
  <dict>
    <entry name="key">
      <number>15</number>
    </entry>
  </dict>
</config>
```

## ❌ Если что-то не работает

1. **Ошибка компиляции**: Выполните `mvn clean compile`
2. **Файл не найден**: Проверьте путь к файлу (относительно корня проекта)
3. **Синтаксическая ошибка**: Проверьте формат файла согласно README.md

Подробная инструкция: `INTELLIJ_SETUP.md` и `README.md`

